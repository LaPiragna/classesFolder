package ClasesGenericas.OperacionesMatematicas;

import java.util.ArrayList;

public class OperacionesMatematicas<E, Y> {
    /*Crear una clase genérica llamada "OperacionMatematica" que pueda realizar operaciones matemáticas simples
    (suma, resta, multiplicación, división) con cualquier tipo de número (enteros, decimales, etc.).
    Debe tener métodos para cada operaci
    }
     */
    public OperacionesMatematicas() {
    }
    /*
    public E sum(E x, E y) throws InvalidType{
        if ((x instanceof Integer) && (y instanceof Integer)) {
            return x + y;
        } else {
            throw new InvalidType();
        }
    }
     */
}
